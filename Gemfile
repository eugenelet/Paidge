source "https://rubygems.org"

# This mimics GitHub's exact build environment
gem "github-pages", group: :jekyll_plugins

# This is required for newer versions of Ruby to run local servers
gem "webrick"